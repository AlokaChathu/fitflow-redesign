# Database

**PostgreSQL** is the primary database for authoritative structured data such as users, workout plans,
workout history, nutrition records, goals, subscriptions, permissions, and consent-related information.

The `migrations/` and `schema/` folders are prepared for future implementation.
