# Frontend

- `mobile/` – React Native + TypeScript mobile application
- `web/` – React + TypeScript web application

The two clients can share TypeScript models, validation logic, API clients, and selected reusable code.
